sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t apuravpatil/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true apuravpatil/react-test npm test

after_success:
  - docker build -t apuravpatil/multi-client ./client
  - docker build -t apuravpatil/multi-nginx ./nginx
  - docker build -t apuravpatil/multi-server ./server
  - docker build -t apuravpatil/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push apuravpatil/multi-client
  - docker push apuravpatil/multi-nginx
  - docker push apuravpatil/multi-server
  - docker push apuravpatil/multi-worker
